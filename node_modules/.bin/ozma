#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../ozma-tudou/bin/ozma" "$@"
  ret=$?
else 
  node  "$basedir/../ozma-tudou/bin/ozma" "$@"
  ret=$?
fi
exit $ret
